"""Recursos estáticos do projeto (template, fontes, planilha, regras).

Permite incluir ``assets/regras/`` na wheel via ``package-data`` no
``pyproject.toml``. Template e planilha locais continuam sendo paths no
disco; não é obrigatório ``import assets``.
"""
